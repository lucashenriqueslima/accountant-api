# Variáveis lidas pelo docker-compose.yml.
# Copie para .env na raiz: cp .env.example .env

# MySQL
DB_DATABASE=forte_contabilidade
DB_PORT=3306

# MailHog (servidor SMTP de testes)
MAIL_PORT=1025
MAILHOG_UI_PORT=8025
